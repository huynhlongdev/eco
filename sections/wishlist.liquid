{%- assign  products = search.results -%}

{% if products.size > 1 %}
  <div class="wishlist grid grid-cols-4 gap-4">
    {%- for product in search.results -%}
      {% render 'content-product', product: product %}
    {% endfor %}
  </div>
{% else %}
  <div class="wishlist-empty text-center">
    <div class="mx-auto mb-6 text-center">
      {%  render 'icon-wishlist' %}
    </div>
    
    <h4 class="mb-3">Wishlist is empty.</h4>
    <p>You don't have any products in the wishlist yet.<br>
      You will find a lot of interesting products on our "Shop" page.</p>

    <a href="{{ routes.all_products_collection_url }}" class="lp-btn mt-5 px-6 py-3 inline-flex">Continue shopping</a>
  </div>
{% endif %}


<style>
  #wishlist-container{
    padding-top: 120px;
    padding-bottom: 120px;
  }
  #wishlist-container .icon{
    width: 60px;
    margin: 0 auto 24px;
  }
  
</style>